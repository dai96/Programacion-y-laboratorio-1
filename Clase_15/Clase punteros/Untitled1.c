punteros= variables
variables que se relacionan con la direccion de memoria

un int=4bytes

un char=1byte

stack=parte de la memoria (apilamiento)
se apilan memorias
la memoria ram esta divivdida en distintos secmentos codigo, estatica, hip o monton espacio libre y pila solo usamos la
pila por el momento, es un segmento estatico, tiene un tamaño definido
cada vez que se recarga una funcuion recursiva

array siempre se pasa con referencia...

un puntero es una variable que guarda la dire de memoria (puede ser de una variable)

char* x int* y

*x se acede al valor

int getint(int*valor)
int x;
int numero:
x=getinnt(&numero);

main nt*x=null
int v=10;
x=&v;
print f("%d",v);
*x=90; (v=90)

